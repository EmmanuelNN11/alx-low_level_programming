#include "main.h"

/**
 * mul - prints out the result of multiplication between two numbers
 * @x: first number
 * @y: second number
 * Return: the value of the multiplication between x and y
 */

int mul(int x, int y)
{
	return (x * y);
}
